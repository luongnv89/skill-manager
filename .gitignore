node_modules/
dist/
bun.lock

# Temp files from runInlineTs (tests/e2e). Cleaned up by a finally block
# on success; listing them here keeps a SIGKILL'd test runner from leaving
# visible untracked files in the repo root.
.asm-inline-ts-*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Environment
.env
.env.local
.env.*.local

# Coverage
coverage/

# Website (generated at build time by `tsx scripts/build-catalog.ts` and
# `vite build`, both run by .github/workflows/deploy-website.yml before
# the Pages upload). Kept out of git to avoid noisy diffs and keep
# data/skill-index/*.json (catalog) + website-src/ (React app) as the
# single sources of truth.
website/catalog.json
website/bundles.json
website/skills.min.json
website/search.idx.json
website/skills/
website/assets/favicon.svg
website/assets/logo-mark.svg
# Vite build output (React app bundle)
website/index.html
website/assets/index-*.js
website/assets/index-*.css


# dev tools
.claude
.opencode
.agents/
openspec/
docs/IMPLEMENTATION_PLAN.md
TODOS.md
CODE_REVIEW.md
competitor-analysis.md

.gitissue
.gstack/
