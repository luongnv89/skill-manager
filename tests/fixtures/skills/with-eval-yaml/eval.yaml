# Deterministic eval spec for `with-eval-yaml`.
#
# Graders run against SKILL.md by the built-in deterministic provider.
# No LLM, no subprocess.
name: with-eval-yaml
threshold: 0.8

graders:
  - id: markdown-shape
    kind: contains
    needle: "## "
